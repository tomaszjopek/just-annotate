import { CreateProjectModalComponent } from './create-project-modal.component'

describe('CreateProjectModalComponent', () => {
  it('should mount', () => {
    cy.mount(CreateProjectModalComponent)
  })
})